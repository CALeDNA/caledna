default: &default
  adapter: postgis
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: caledna_development

test:
  <<: *default
  database: caledna_test

production:
  <<: *default
  database: caledna_production
  username: caledna
  url: <%= ENV.fetch('DATABASE_URL', '').sub(/^postgres/, "postgis") %>
