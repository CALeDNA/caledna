<% content_for(:title) { 'Import Kobo Projects' } %>

<header class="main-content__header" role="banner">
  <h1 class="main-content__page-title">
    <%= content_for(:title) %>
  </h1>
</header>

<section class="main-content__body">
  <%= form_tag(admin_labwork_import_projects_path, method: "post") do %>
    <%= submit_tag("Import Projects") %>
  <% end %>

  <table class="no-auto-link">
    <tr>
      <th>Name</th>
      <th>Records</th>
      <th>Last Import</th>
      <th></th>
    </tr>
    <% @projects.each do |project| %>
    <tr>
      <td><%= link_to project.name, admin_field_data_project_path(project.id) %></td>
      <td><%= project.samples.count %></td>
      <td><%= project.last_import_date %></td>
      <td>
        <%= form_tag(admin_labwork_import_samples_path(project), method: "post") do %>
          <%= submit_tag("Import Samples") %>
        <% end %>
      </td>
    </tr>
    <% end %>
  </table>

</section>
