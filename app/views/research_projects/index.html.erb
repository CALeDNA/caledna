<h1>Research Projects</h1>
<p class="page-description">
  Here's some of scientific reasearch projects that are studying our eDNA data.
</p>

<table class="table">
  <tr>
    <th>Name</th>
    <th>Approved Samples</th>
  </tr>

  <% @projects.each do |project| %>
    <tr class="clickable_row" data-path='<%=  research_project_path(id: project['id'])%>' >
      <td><%= project['name'] %></td>
      <td><%= project['count'] %></td>
    </tr>
  <% end %>
</table>

<%= paginate @projects %>
