<table class="table">
  <tr>
    <th>Organisms from ASV</th>
    <th>Sample</th>
    <th>Latitude</th>
    <th>Longitude</th>
    <th>Field Data Project</th>
  </tr>

  <% samples.each do |sample| %>
    <tr>
      <td><% links = sample.taxons.map do |t|  %>
      <% link_to t.first, taxa_path(id: t.last, view: params[:view]) %>
      <% end %>
      <%= raw links.join('<br> ')%>
      </td>
      <td><%= link_to sample.barcode, sample_path(id: sample.id) %></td>
      <td><%= sample.latitude %> </td>
      <td><%= sample.longitude %></td>
      <td><%= link_to sample.project_name,  field_data_project_path(id: sample.field_data_project_id) %></td>
    </tr>
  <% end %>
</table>

<%= paginate samples %>
