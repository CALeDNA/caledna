<h1>Highlights</h1>
<% @higlights.each do |taxon| %>
 <li>
  <%= link_to taxon.complete_name, taxa_path(taxon, view: params[:view]) %>
  <%= taxon.common_name %>
  </li>
<% end %>

<h1>Most Commonly Found</h1>
<% @top_taxa.each do |taxon| %>
 <li>
  <%= link_to taxon.complete_name, taxa_path(taxon, view: params[:view]) %>
  <%= taxon.common_name %>
  </li>
<% end %>
