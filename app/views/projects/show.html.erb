<h1><%= @project.name %></h1>

<p><%= @project.description %></p>

<p>
  Samples collected: <%= @project.samples.count %>,
  <%= link_to samples_path(project_id: @project.id) do %>
    <span class=" glyphicon glyphicon-list-alt" aria-hidden="true"></span> View
  <% end %>
</p>
<p>
  Samples being analyzed: <%= @project.samples.analyzed.count %>,
  <%= link_to samples_path(project_id: @project.id, status: :analyzed) do %>
    <span class=" glyphicon glyphicon-list-alt" aria-hidden="true"></span> View
  <% end %>
</p>
<p>
  Samples with DNA results: <%= @project.samples.results_completed.count %>,
  <%= link_to samples_path(project_id: @project.id, status: :results_completed) do %>
    <span class=" glyphicon glyphicon-list-alt" aria-hidden="true"></span> View
  <% end %>
</p>
